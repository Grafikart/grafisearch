package templates;

import "fmt"

templ SearchPage(wallpaper string, bangs map[string]string) {
	@Layout("GrafiSearch") {
		@templ.Raw(fmt.Sprintf(`
		<style>
			body {
				background-image: url(%s);
			}
		</style>
		`, wallpaper))
		<div id="search"/>
		@templ.JSONScript("bangs", bangs)
	}
}
